<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Quoting Dojo</title>
  </head>
  <body>
  <h2>Here are the awesome quotes!</h2>
  <hr>

  <% if(typeof(errors) != 'undefined') { %>
     <% for (var x in errors) { %>
      <h3><%= errors[x].message %></h3>
    <% } %>
  <% } %>

  <% if(typeof(errors) == 'undefined') { %>
    <% for (var i = 0; i < quotes.length; i++) { %>
      <p><strong><%= quotes[i].name %></strong> : <em><%= quotes[i].quote %></em></p>
      <% } %>
  <% } %>

  <a href="/"><button type="button" name="button">Back to Quoting Dojo</button></a>

  </body>
</html>
