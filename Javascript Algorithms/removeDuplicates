function removeDuplicates(arr){
  new_array = [];
  new_array[0] = arr[0];
  for(var i=1; i<arr.length; i++){
    if(arr[i] !== arr[i-1]){
      new_array[new_array.length] = arr[i];
    }
  }
  return new_array;
}
