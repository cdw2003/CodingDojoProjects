function valuesGreaterThanSecond(arr){
var count = 0;
var new_arr = [];
for(var i=0; i<arr.length; i++){
  if(arr.length >2){
    if(arr[i] > arr[1]){
      new_arr.push(arr[i]);
      count += 1;
      }
    }
  }
  console.log(new_arr)
  return count
}
